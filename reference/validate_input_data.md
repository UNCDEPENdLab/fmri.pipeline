# helper function to verify contents of subject, run, block, trial, and subtrial data

helper function to verify contents of subject, run, block, trial, and
subtrial data

## Usage

``` r
validate_input_data(df, vm, lg, level = "trial")
```
