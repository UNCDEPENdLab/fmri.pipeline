# Setup a first-level SPM model using a build_design_matrix object

Setup a first-level SPM model using a build_design_matrix object

## Usage

``` r
spm_l1_model(
  id = NULL,
  session = NULL,
  l1_confound_files = NULL,
  d_obj,
  gpa,
  model_name = NULL,
  run_nifti = NULL,
  run_numbers = NULL,
  run_sessions = NULL,
  source_run_numbers = NULL,
  l1_session_mode = NULL,
  execute_spm = FALSE
)
```

## Arguments

- id:

  subject id.

- session:

  session number.

- l1_confound_files:

  optional character vector of nuisance regressor files, one per run, to
  include in SPM's multi_reg field

- d_obj:

  a single-subject design matrix object generated by build_design_matrix

- gpa:

  a gpa (glm_pipeline_arguments) object generated by setup_glm_pipeline

- model_name:

  a string indicating the model name within `gpa` to setup. If you wish
  to setup multiple models, this is handled upstream in
  setup_l1_models.R

- run_nifti:

  optional character vector of NIfTI filenames used in l1 analysis.
  Prefer to pass these via the build_design_matrix object in \$run_nifti

- run_numbers:

  optional integer vector of run numbers corresponding to `run_nifti`

- run_sessions:

  optional session vector corresponding to `run_nifti`.

- source_run_numbers:

  optional source run numbers for pooled/session models.

- l1_session_mode:

  optional session handling mode for SPM L1 setup.

- execute_spm:

  whether to execute SPM setup/estimation immediately. Default: FALSE

## Author

Michael Hallquist
