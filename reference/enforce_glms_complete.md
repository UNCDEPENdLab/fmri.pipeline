# Helper function to create named data.frame from a set of objects

Helper function to create named data.frame from a set of objects

## Usage

``` r
enforce_glms_complete(gpa, level = 1L, lg = NULL, glm_software = NULL)
```

## Details

This function helps with the problem of having several vectors in the
workspace that you want to combine with data.fram
