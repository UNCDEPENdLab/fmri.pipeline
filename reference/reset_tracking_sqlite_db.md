# Internal helper function to reset tracking SQLite database

Internal helper function to reset tracking SQLite database

## Usage

``` r
reset_tracking_sqlite_db(sqlite_db)
```

## Arguments

- sqlite_db:

  Path to SQLite database used for tracking
