# helper function to copy any missing fields in target

helper function to copy any missing fields in target

## Usage

``` r
populate_defaults(target = NULL, defaults)
```

## Arguments

- target:

  a named list to be populated by defaults if fields are missing

- defaults:

  a named list containing default values
