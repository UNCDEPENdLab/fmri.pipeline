# Validate an FWE correction specification

Validate an FWE correction specification

## Usage

``` r
validate_fwe_spec(x)
```

## Arguments

- x:

  an `fwe_spec` or an equivalent plain list.

## Value

`x`, invisibly, if validation succeeds. Otherwise an error is raised
with the invalid field.
