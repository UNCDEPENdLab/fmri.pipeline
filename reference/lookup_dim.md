# Helper function to obtain the dimensions of a NIfTI file

Helper function to obtain the dimensions of a NIfTI file

## Usage

``` r
lookup_dim(nifti)
```

## Arguments

- nifti:

  a nifti file

## Value

a named vector of dimensions for the input (dim_x, dim_y, dim_z, dim_t)
