# Helper function to output l2_models into '.yaml' object

Helper function to output l2_models into '.yaml' object

## Usage

``` r
get_l2_config(gpa)
```

## Arguments

- gpa:

  the `gpa` object

## Value

a yaml document of l2_models for an object
