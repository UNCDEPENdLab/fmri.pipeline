# Summarize the contents of the subject data

Summarize the contents of the subject data

## Usage

``` r
# S3 method for class 'bg_block_data'
summary(object, ...)
```

## Arguments

- object:

  A `bg_block_data` object containing block-level data.

- ...:

  ignored.

## Details

This is typically run using `summary(gpa$block_data)`
