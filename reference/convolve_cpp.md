# Internal function to convolve two vectors using nested for loop

Internal function to convolve two vectors using nested for loop

## Arguments

- a:

  A vector

- b:

  A vector

## Value

A vector containing the convolution of a and b

## Details

This is an internal function Same result as, in R: convolve(a, b,
conj=TRUE, type="open")
