# Validate a compiled FWE plan

Validate a compiled FWE plan

## Usage

``` r
validate_fwe_plan(x, require_inputs = FALSE)
```

## Arguments

- x:

  an `fwe_plan` or equivalent list.

- require_inputs:

  if `TRUE`, require every task to have all inputs.

## Value

`x`, invisibly, when valid.
