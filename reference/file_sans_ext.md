# internal function for returning a file (and path) without its extension at present, it returns NA if no recognized extension is there

internal function for returning a file (and path) without its extension
at present, it returns NA if no recognized extension is there

## Usage

``` r
file_sans_ext(file, withdot = TRUE)
```
