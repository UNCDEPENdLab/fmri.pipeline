# Summarize the contents of the subject data

Summarize the contents of the subject data

## Usage

``` r
# S3 method for class 'bg_subject_data'
summary(df)
```

## Arguments

- df:

  A `bg_subject_data` object containing subject-level data.

## Details

This is typically run using `summary(gpa$trial_data)`
