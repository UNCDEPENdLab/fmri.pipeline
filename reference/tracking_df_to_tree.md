# Helper to convert a tracking dataframe into a data.tree object

Helper to convert a tracking dataframe into a data.tree object

## Usage

``` r
tracking_df_to_tree(tracking_df)
```

## Arguments

- tracking_df:

  a data.frame object extracted from the job tracking SQLite databse
