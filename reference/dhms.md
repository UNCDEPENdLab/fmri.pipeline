# small helper function to parse duration syntax of days-hours:minutes:seconds into lubridate duration object

small helper function to parse duration syntax of
days-hours:minutes:seconds into lubridate duration object

## Usage

``` r
dhms(str)
```

## Arguments

- str:

  string containing a duration that may include a days specification
