# Prepare signals for expansion

Ensures that beta_series is populated with TRUE or FALSE (default FALSE)
for each signal.

## Usage

``` r
prepare_signals_for_expansion(signals)
```

## Arguments

- signals:

  list of signal specifications

## Value

signals list with beta_series explicitly set
