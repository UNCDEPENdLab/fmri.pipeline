# Helper function to better summarize GPA object's l1_models' models

Helper function to better summarize GPA object's l1_models' models

## Usage

``` r
# S3 method for class 'l1_model_set_models'
summary(object, ...)
```

## Arguments

- object:

  the `l1_model_set_models` object

- ...:

  ignored.

## Value

a readable summary of the given gpa object's l1_models' models
