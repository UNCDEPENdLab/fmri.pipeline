# helper function to get generic busy timeout

helper function to get generic busy timeout

## Usage

``` r
get_default_sqlite_busy_timeout(ms = T)
```

## Arguments

- ms:

  Logical. Return in milliseconds? If FALSE, returns in seconds
