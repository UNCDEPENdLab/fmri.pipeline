% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolve_reglin.R
\name{deconvolve_reglin}
\alias{deconvolve_reglin}
\title{Regularized linear deconvolution of BOLD time series}
\usage{
deconvolve_reglin(
  BOLDobs,
  kernel,
  lambda = NULL,
  lambda_grid = 10^seq(-4, 2, length.out = 20),
  penalty = c("diff2", "diff1", "ridge"),
  kernel_grid = NULL,
  hrf_lags = 0L,
  tune_by = c("global", "signal"),
  normalize = TRUE,
  demean = TRUE,
  trim_kernel = TRUE,
  ridge_floor = 1e-08,
  return_diagnostics = FALSE,
  prewhiten_gcv = FALSE,
  prewhiten_rho = NULL,
  backend = c("cpp", "r", "wiener"),
  wiener_shrinkage = c("scalar", "psd"),
  wiener_psd_smooth = 7L,
  wiener_psd_floor = 1e-08,
  gcv_rule = c("min", "1se")
)
}
\arguments{
\item{BOLDobs}{matrix of observed BOLD time series (n_timepoints x n_signals)
or a numeric vector.}

\item{kernel}{assumed HRF kernel. A vector or single-column matrix.}

\item{lambda}{regularization strength. If \code{NULL}, chosen from
\code{lambda_grid} by generalized cross-validation.}

\item{lambda_grid}{candidate regularization strengths used when
\code{lambda} is \code{NULL}.}

\item{penalty}{regularization penalty: \code{"diff2"} for smooth curvature,
\code{"diff1"} for smooth first differences, or \code{"ridge"} for simple
amplitude shrinkage.}

\item{kernel_grid}{optional list or matrix of candidate HRF kernels. If
supplied, \code{kernel} and \code{hrf_lags} are ignored for tuning.}

\item{hrf_lags}{integer shifts, in TR bins, applied to \code{kernel}. Negative
values make the candidate HRF peak earlier; positive values make it later.}

\item{tune_by}{whether to tune \code{lambda}/HRF globally across all signals
or independently for each signal.}

\item{normalize}{whether to z-score \code{BOLDobs} before deconvolution.
Default: \code{TRUE}.}

\item{demean}{whether to subtract column means before deconvolution. Ignored
when \code{normalize = TRUE}, because z-scoring already demeans.}

\item{trim_kernel}{whether to remove the initial \code{length(kernel) - 1}
latent samples, corresponding to pre-observation HRF support.}

\item{ridge_floor}{small diagonal ridge added for numerical stability.}

\item{return_diagnostics}{whether to return a list containing diagnostics
rather than only the deconvolved matrix.}

\item{prewhiten_gcv}{whether to estimate AR(1) autocorrelation and use a
prewhitened objective for lambda/HRF selection and deconvolution.}

\item{prewhiten_rho}{optional AR(1) coefficient used when
\code{prewhiten_gcv = TRUE}. If \code{NULL}, a pooled median AR(1)
coefficient is estimated from first-pass reconstruction residuals.}

\item{backend}{implementation backend. \code{"cpp"} uses the RcppArmadillo
regularized inverse implementation; \code{"r"} uses the reference R
implementation; \code{"wiener"} uses a frequency-domain Wiener-style
deconvolution for comparison.}

\item{wiener_shrinkage}{shrinkage strategy for \code{backend = "wiener"}.
\code{"scalar"} uses \code{Conj(H) / (|H|^2 + lambda)}. \code{"psd"}
estimates smoothed signal and residual noise spectra from a scalar Wiener
first pass, then uses frequency-dependent shrinkage.}

\item{wiener_psd_smooth}{odd integer smoothing width for the estimated
signal/noise spectra in \code{wiener_shrinkage = "psd"}.}

\item{wiener_psd_floor}{lower bound for estimated spectra.}

\item{gcv_rule}{lambda/HRF selection rule. \code{"min"} selects the minimum
mean GCV candidate. \code{"1se"} selects the largest lambda whose mean GCV
is within one standard error of the minimum. The standard error is computed
across signal columns for \code{tune_by = "global"}.}
}
\value{
A matrix of deconvolved time series, or a list with \code{activity}
  and tuning diagnostics when \code{return_diagnostics = TRUE}.
}
\description{
Estimate a continuous latent activity time series by solving a regularized
inverse convolution problem for one or more BOLD signals.
}
