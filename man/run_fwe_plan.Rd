% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fwe_execution.R
\name{run_fwe_plan}
\alias{run_fwe_plan}
\title{Execute a compiled FWE plan}
\usage{
run_fwe_plan(
  plan,
  scheduler = NULL,
  force = FALSE,
  dry_run = FALSE,
  allow_blocked = FALSE,
  worker_script = NULL,
  commands_per_cpu = 1L,
  cpus_per_job = 1L,
  memgb_per_command = 8,
  time_per_command = "12:00",
  sched_args = NULL,
  env_variables = NULL,
  wait_jobs = NULL,
  stop_on_error = TRUE,
  persist_results = TRUE
)
}
\arguments{
\item{plan}{an \code{fwe_plan} or path to an RDS plan.}

\item{scheduler}{execution backend. Defaults to the scheduler compiled into
the plan.}

\item{force}{rerun tasks whose required artifacts already exist.}

\item{dry_run}{compile commands without creating directories, executing, or
submitting jobs.}

\item{allow_blocked}{skip blocked tasks instead of rejecting the run.}

\item{worker_script}{optional method worker override. Primarily useful for
development and testing.}

\item{commands_per_cpu, cpus_per_job, memgb_per_command, time_per_command}{resource settings passed to \code{cluster_submit_shell_jobs()} for Slurm
and TORQUE execution.}

\item{sched_args, env_variables, wait_jobs}{optional scheduler settings passed
to \code{cluster_submit_shell_jobs()}. Named \code{env_variables} are also
applied during local execution.}

\item{stop_on_error}{raise an \code{fwe_execution_error} if a local command
fails, does not produce all required artifacts, or a scheduler submission
fails. The condition contains the run report in its \code{run} field.}

\item{persist_results}{if \code{TRUE}, real executions and submissions update
the persistent artifact manifest and RDS result snapshot. Dry runs never
write result files.}
}
\value{
an \code{fwe_run} containing the execution report and refreshed
  \code{fwe_plan}.
}
\description{
Execute ready tasks locally or submit them through Slurm or TORQUE. Completed
tasks are skipped unless \code{force = TRUE}. Local execution is synchronous
and returns a refreshed plan; scheduler execution returns submitted job IDs
and can later be refreshed with \code{refresh_fwe_plan()}.
}
