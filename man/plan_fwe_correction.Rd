% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fwe_plan.R
\name{plan_fwe_correction}
\alias{plan_fwe_correction}
\title{Compile an FWE specification into an executable plan}
\usage{
plan_fwe_correction(
  gpa,
  spec,
  output_directory = NULL,
  require_inputs = TRUE,
  source = c("auto", "setup", "cache", "filesystem"),
  cache_dir = NULL,
  refresh_status = FALSE,
  lg = NULL
)
}
\arguments{
\item{gpa}{a \code{glm_pipeline_arguments} containing group-analysis output
metadata.}

\item{spec}{an \code{fwe_spec} or equivalent list.}

\item{output_directory}{root directory for this correction. The default is
\code{<gpa$output_directory>/fwe/<spec$name>}.}

\item{require_inputs}{if \code{TRUE}, error when a required statistic, mask,
or FSL smoothness file is missing. If \code{FALSE}, return a plan with
blocked tasks for inspection.}

\item{source, cache_dir, refresh_status}{passed to
\code{resolve_fwe_targets()}.}

\item{lg}{optional \code{lgr::Logger}.}
}
\value{
an object of class \code{fwe_plan} containing \code{analyses},
  \code{tasks}, and expected \code{artifacts} tables.
}
\description{
Resolve an FWE specification against completed GLM outputs and compile it
through a method adapter. Planning is side-effect free: output directories
and result files are described but not created. The pTFCE adapter creates one
task per selected z-statistic while retaining shared GFEAT-level inputs in an
analysis table.
}
