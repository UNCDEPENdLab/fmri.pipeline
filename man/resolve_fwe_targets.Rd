% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fwe_specification.R
\name{resolve_fwe_targets}
\alias{resolve_fwe_targets}
\title{Resolve an FWE specification to concrete group-analysis targets}
\usage{
resolve_fwe_targets(
  gpa,
  spec,
  require_existing = TRUE,
  require_complete = TRUE,
  source = c("auto", "setup", "cache", "filesystem"),
  cache_dir = NULL,
  refresh_status = FALSE,
  lg = NULL
)
}
\arguments{
\item{gpa}{a \code{glm_pipeline_arguments} with level-3 FSL setup metadata.}

\item{spec}{an \code{fwe_spec} or equivalent list.}

\item{require_existing}{if \code{TRUE}, fail when a selected statistic image
is absent. If \code{FALSE}, unresolved output paths are retained for
planning and preview.}

\item{require_complete}{if \code{TRUE}, fail when a selected group analysis
is not complete.}

\item{source, cache_dir, refresh_status}{passed to \code{lookup_feat_outputs()}.}

\item{lg}{optional \code{lgr::Logger}.}
}
\value{
a data.frame of class \code{fwe_target_set}, with one row per selected
  group contrast and stable \code{target_key} and \code{target_id} columns.
}
\description{
Resolve semantic model and contrast selectors against the canonical FSL
output inventory returned by \code{lookup_feat_outputs()}. Resolution never
constructs FEAT paths independently.
}
